name: test
on:
  push:
    branches: [ $default-branch ]
  pull_request:
    branches: [ $default-branch ]
runs-on: [self-hosted, linux]
jobs:
  test:
    name: test
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: hello
      run: echo "hello"
